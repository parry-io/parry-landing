node_modules
.next
out
.env
.env.local

# Data-room generated credentials (local only — never commit)
dataroom-credentials.local.txt

# Firebase
.firebase/

# TypeScript build info
*.tsbuildinfo

# Claude worktree config
.claude/
